[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

# The Next.js runtime plugin is auto-installed by Netlify when it detects
# Next.js in package.json. No explicit [[plugins]] entry needed.
